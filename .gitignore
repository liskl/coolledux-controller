# Compiled binaries (root only)
/coolledux-controller
/bletest
*.exe
*.test

# Runtime logs
service.log
nohup.out

# Debug/test tool sources (not part of the service)
cmd/bletest/
cmd/crctest/
cmd/digit-dump/
scripts/matrix-probe.py

# IDE and editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Go
vendor/
coverage.out
coverage.html

# Config with secrets (example is tracked)
config.yaml
!config.example.yaml

# Claude Code local settings
.claude/

# Reference APK and decompiled source (third-party, not for distribution)
references/

# Assets extracted from the CoolLED 1248 APK. See each directory's README for
# extraction instructions. Not checked in due to third-party origin.
internal/text/fonts/*.bin
internal/controller/assets/*.gif

# Local-only Claude instructions
CLAUDE.local.md
